{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "dev": "uvicorn app.main:socket_app --host 0.0.0.0 --port 8000 --reload",
    "start": "uvicorn app.main:socket_app --host 0.0.0.0 --port 8000",
    "migrate": "alembic upgrade head",
    "migrate:new": "alembic revision --autogenerate -m"
  }
}
